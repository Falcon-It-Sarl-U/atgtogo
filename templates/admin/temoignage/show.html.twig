{% extends 'base.html.twig' %}

{% block title %}Temoignage{% endblock %}

{% block body %}
    <h1>Temoignage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoignage.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ temoignage.message }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ temoignage.auteur }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ temoignage.profession }}</td>
            </tr>
        </tbody>
    </table>

    <a class='btn btn-success' href="{{ path('app_admin_temoignage_index') }}">back to list</a>

    <a class='btn btn-warning' href="{{ path('app_admin_temoignage_edit', {'id': temoignage.id}) }}">edit</a>

    {{ include('admin/temoignage/_delete_form.html.twig') }}
{% endblock %}
