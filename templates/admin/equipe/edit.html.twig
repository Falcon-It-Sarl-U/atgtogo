{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Equipe{% endblock %}

{% block body %}
    <h1>Edit Equipe</h1>

    {{ include('admin/equipe/_form.html.twig', {'button_label': 'Update'}) }}

    <a class='btn btn-success' href="{{ path('app_admin_equipe_index') }}">back to list</a>

    {{ include('admin/equipe/_delete_form.html.twig') }}
{% endblock %}
