{% extends 'base.html.twig' %}

{% block title %}Certification{% endblock %}

{% block body %}
    <h1>Certification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certification.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ certification.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ certification.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ certification.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class='btn btn-success' href="{{ path('app_admin_certification_index') }}">back to list</a>

    <a class='btn btn-warning' href="{{ path('app_admin_certification_edit', {'id': certification.id}) }}">edit</a>

    {{ include('admin/certification/_delete_form.html.twig') }}
{% endblock %}
